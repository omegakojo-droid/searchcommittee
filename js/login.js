document.getElementById('loginForm').addEventListener('submit',async e=>{e.preventDefault();const email=document.getElementById('email').value;const password=document.getElementById('password').value;const r=await supabase.auth.signInWithPassword({email,password});if(r.error){document.getElementById('message').textContent=r.error.message;return;}const u=(await supabase.auth.getUser()).data.user;const q=await supabase.from('users').select('roles(role_name)').eq('auth_user_id',u.id).single();if(q.error){document.getElementById('message').textContent=q.error.message;return;}location.href=q.data.roles.role_name==='Panel Member'?'member/dashboard.html':'admin/dashboard.html';});